export default {
  entities: {
    /*
    Category: {
      form: {
        fields: '__all__'
      },
      list: {
        list_display: ['id', 'type', 'name', 'parent', 'enabled', 'sequence']
      }
    }
    */
  }
}
