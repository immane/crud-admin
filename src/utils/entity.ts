// Deprecated compatibility shim. New code must import
// CrudSkeletonAdapter from '@/easyadmin/adapters/crudskeleton/CrudSkeletonAdapter'.
// This module is kept only until the remaining test imports migrate (Phase 6),
// then it will be deleted.
export { default, CrudSkeletonAdapter as EntityManage } from '@/easyadmin/adapters/crudskeleton/CrudSkeletonAdapter'
