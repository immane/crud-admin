// Canonical field-config re-export.
// src/types/admin.ts remains the single source until every consumer migrates
// into easyadmin; do not duplicate these interfaces here.
export * from '@/types/admin'
